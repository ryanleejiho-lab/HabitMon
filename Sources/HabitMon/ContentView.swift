import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("HabitMon")
            .font(.largeTitle)
            .padding()
            .frame(width: 480, height: 360)
    }
}

#Preview {
    ContentView()
}
